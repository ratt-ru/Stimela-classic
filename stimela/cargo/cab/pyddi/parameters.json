{
    "task": "pyddi", 
    "base": "stimela/pyddi", 
    "tag": "1.1.3", 
    "description": "An Astronomical Image Fidelity Assessment Tool", 
    "prefix": "--", 
    "binary": "pyddi", 
    "msdir": false, 
    "use_graphics": true,
    "parameters": [
        {
            "info": "Image of interest.",
            "name": "img",
            "io": "input",
            "default": null,
            "dtype": "file",
            "required": true
        }, 
        {
            "info": "The psf image.",
            "name": "psf",
            "io": "input",
            "default": null,
            "dtype": "file",
            "required": false
        }, 
        {
            "info": "Sky model as LSM/txt. Must be in Tigger format: #format:name, ra_d, dec_d, i",
            "name": "cat",
            "io": "input",
            "default": null,
            "dtype": "file",
            "required": false
        }, 
        {
            "info": "Flux threshold. Regions in an image with flux > fth * noise are considered.", 
            "default": 10.0,
            "required": false, 
            "name": "flux-thresh", 
            "dtype": "float"
        }, 
        {
            "info": "Local variance threshold. Sources with varinace > vth * noise are considered.",
            "name": "variance-thresh",
            "default": 5.0,
            "dtype": "float",
            "required": false
        }, 
        {
            "info": "The size of the region to compute the local variance. E.g vsize=10, gives a region of size = 10 * resolution. The resolution is in pixels.",
            "name": "var-size",
            "default": 10,
            "dtype": "int",
            "required": false
        }, 
        {
            "info": "Correlation threshold. Sources with correlation factor > cth are considered.",
            "dtype": "float",
            "default": 0.5,
            "name": "correlation-thresh",
            "required": false
        }, 
        {
            "info": "The size of the region to compute correlation. see vsize.",
            "default": 5,
            "required": false,
            "name": "corr-size",
            "dtype": "int"
        }, 
        {
            "info": "The size of the region to group the pixels, in terms of psf-size. The psf is in degrees. e.g gpix=20, gives 20xpsf.",
            "default": 20.0,
            "required": false,
            "name": "group-pix",
            "dtype": "float"
        },
        {
            "info": "Use -cat for the identification and not only -i.",
            "default": false,
            "required": false,
            "name": "use-cat",
            "dtype": "bool"
        },
        {
            "info": "The prefix for the output file containing directions in RA, DEC both in degrees, and peak flux of the pixels.",
            "default": null,
            "required": false,
            "name": "outpref",
            "dtype": "str"
        }
    ]
}
